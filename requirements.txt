rich>=13.0
fastapi>=0.110
uvicorn[standard]>=0.27
itsdangerous>=2.0
# Postgres (used in production / serverless when POSTGRES_URL is set):
psycopg[binary]>=3.1
# Real Gmail (read-only) connect:
google-auth>=2.23
google-auth-oauthlib>=1.1
google-api-python-client>=2.100
